<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.ssi.entrega2">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".view.EditPedidoActivity"></activity>
        <activity android:name=".view.EditProdutoActivity" />
        <activity android:name=".view.CadProdutoActivity" />
        <activity android:name=".view.SobreActivity" />
        <activity android:name=".view.BuscaActivity" />
        <activity android:name=".view.PedidoActivity" />
        <activity android:name=".view.ProdutoActivity" />
        <activity android:name=".view.CadPedidoActivity" />
        <activity android:name=".view.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <data android:scheme="geo" />
                <category
                    android:name="android.intent.category.DEFAULT" />
            </intent-filter>
        </activity>
    </application>

</manifest>